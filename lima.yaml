minimumLimaVersion: "1.0.0"

vmType: "vz"
arch: "aarch64"
mountType: "virtiofs"

cpus: 2
memory: "2GiB"
disk: "10GiB"

images:
  - location: "https://cloud-images.ubuntu.com/releases/24.04/release-20260814/ubuntu-24.04-server-cloudimg-arm64.img"
    arch: "aarch64"
    digest: "sha256:4a281a921b8d7db952895ab619736f10efe9f63e111fa5b5779ed18f023818aa"
  - location: "https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-arm64.img"
    arch: "aarch64"

# `make devbox` replaces this value with the host checkout path.
param:
  projectDir: "/path/to/replay"

mounts:
  - location: "{{.Param.projectDir}}"
    mountPoint: "/workspace/replay"
    writable: true

provision:
  - mode: system
    script: |
      #!/bin/bash
      set -eux -o pipefail
      export DEBIAN_FRONTEND=noninteractive

      apt-get update
      apt-get install -y --no-install-recommends \
        binutils \
        bison \
        build-essential \
        ca-certificates \
        curl \
        gcc \
        git \
        make \
        mercurial
      rm -rf /var/lib/apt/lists/*

  - mode: user
    script: |
      #!/bin/bash
      set -ex -o pipefail

      gvm_revision="dd652539fa4b771840846f8319fad303c7d0a8d2"
      go_version="$(awk '$1 == "go" { print "go" $2; exit }' /workspace/replay/go.mod)"
      test -n "${go_version}"

      if [[ ! -s "${HOME}/.gvm/scripts/gvm" ]]; then
        curl -fsSL "https://raw.githubusercontent.com/moovweb/gvm/${gvm_revision}/binscripts/gvm-installer" |
          bash -s -- "${gvm_revision}"
      fi

      # GVM initialization may return nonzero after defining the gvm function.
      source "${HOME}/.gvm/scripts/gvm" || [[ "$(type -t gvm)" == "function" ]]
      if [[ ! -x "${HOME}/.gvm/gos/${go_version}/bin/go" ]]; then
        gvm install "${go_version}" -B
      fi
      gvm use "${go_version}" --default

probes:
  - mode: readiness
    description: "GVM and the replay Go toolchain are available"
    script: |
      #!/bin/bash
      set -ex -o pipefail

      source "${HOME}/.gvm/scripts/gvm" || [[ "$(type -t gvm)" == "function" ]]
      test -f /workspace/replay/go.mod
      go_version="$(awk '$1 == "go" { print "go" $2; exit }' /workspace/replay/go.mod)"
      test -n "${go_version}"
      test "$(go env GOVERSION)" = "${go_version}"
    hint: |
      GVM, the Go version declared by /workspace/replay/go.mod, or the project mount is unavailable.
      Inspect /var/log/cloud-init-output.log inside the guest.
